{
  "name": "emoji-alchemy-worker",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "check": "node --check src/worker.js && node --check src/telegram-bot.js && node --test",
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
